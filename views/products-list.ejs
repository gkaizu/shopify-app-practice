<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>商品一覧 - Shopify Inventory Alert</title>
  <link rel="stylesheet" href="/css/common.css">
  <link rel="stylesheet" href="/css/products-list.css">
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>📦 商品一覧</h1>

      <div class="summary">
        <div class="summary-text">
          全 <%= products.length %> 商品
        </div>
      </div>
    </div>

    <div class="products-section">
      <h2>Shopify商品データ</h2>

      <% if (products.length === 0) { %>
        <div class="empty-state">
          <div class="empty-state-icon">📦</div>
          <p>商品がありません</p>
        </div>
      <% } else { %>
        <div class="table-wrapper">
          <table>
            <thead>
              <tr>
                <th>商品名</th>
                <th>商品ID</th>
                <th>在庫数</th>
                <th>ステータス</th>
              </tr>
            </thead>
            <tbody>
              <% products.forEach(product => { %>
                <tr>
                  <td class="product-title"><%= product.title %></td>
                  <td><%= product.id %></td>
                  <td><%= product.inventory_quantity %>個</td>
                  <td>
                    <% if (product.inventory_quantity > 50) { %>
                      <span class="inventory-badge high">✅ 在庫充分</span>
                    <% } else if (product.inventory_quantity > 10) { %>
                      <span class="inventory-badge medium">⚠️ 在庫少</span>
                    <% } else { %>
                      <span class="inventory-badge low">🚨 要補充</span>
                    <% } %>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } %>
    </div>

    <div class="actions">
      <a href="/dashboard" class="btn btn-primary">📊 ダッシュボードに戻る</a>
      <a href="/" class="btn btn-secondary">🏠 ホームへ</a>
    </div>
  </div>
</body>
</html>
