<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>在庫チェック結果 - Shopify Inventory Alert</title>
  <link rel="stylesheet" href="/css/common.css">
  <link rel="stylesheet" href="/css/inventory-check.css">
</head>
<body>
  <div class="container-small">
    <div class="header">
      <h1>📊 在庫チェック結果</h1>
      <div class="store-name">ストア: <%= shop_name %></div>

      <div class="summary">
        <div class="summary-text">
          ✅ <%= checked %>件の商品をチェックしました
          <% if (alertCount > 0) { %>
            | 🚨 <strong><%= alertCount %>件</strong> の在庫アラートを送信しました
          <% } else { %>
            | ✨ すべての商品の在庫は十分です
          <% } %>
        </div>
      </div>
    </div>

    <div class="results-section">
      <h2>チェック結果</h2>

      <% if (alerts.length === 0) { %>
        <div class="empty-state">
          <div class="empty-state-icon">✨</div>
          <p>アクティブなアラート設定がありません</p>
        </div>
      <% } else { %>
        <% alerts.forEach(alert => { %>
          <% if (alert.error) { %>
            <div class="alert-card">
              <div class="product-title">商品ID: <%= alert.product_id %></div>
              <p style="color: #f04747; margin-top: 8px;">
                ❌ エラー: <%= alert.error %>
              </p>
            </div>
          <% } else { %>
            <div class="alert-card <%= alert.alerted ? 'warning' : 'success' %>">
              <div class="product-title"><%= alert.product_title %></div>

              <div class="product-details">
                <div class="detail-item">
                  <span class="detail-label">商品ID</span>
                  <span class="detail-value"><%= alert.product_id %></span>
                </div>

                <div class="detail-item">
                  <span class="detail-label">現在の在庫</span>
                  <span class="detail-value <%= alert.alerted ? 'danger' : 'success' %>">
                    <%= alert.current_inventory %>個
                  </span>
                </div>

                <div class="detail-item">
                  <span class="detail-label">閾値</span>
                  <span class="detail-value"><%= alert.threshold %>個</span>
                </div>
              </div>

              <% if (alert.alerted) { %>
                <span class="alert-badge sent">🚨 Slack通知を送信しました</span>
              <% } else { %>
                <span class="alert-badge ok">✅ 在庫は十分です</span>
              <% } %>
            </div>
          <% } %>
        <% }) %>
      <% } %>
    </div>

    <div class="actions">
      <a href="/dashboard?shop_name=<%= encodeURIComponent(shop_name) %>" class="btn btn-primary">
        📊 ダッシュボードに戻る
      </a>
      <a href="/check-inventory?shop_name=<%= encodeURIComponent(shop_name) %>" class="btn btn-secondary">
        🔄 再チェック
      </a>
    </div>
  </div>
</body>
</html>
